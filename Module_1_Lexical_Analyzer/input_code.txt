// Final Test Code for Module 1 Lexical Analyzer
public class Calculator {
    public static void main() {
        float balance = 1000.50;
        int withdrawal = 500;
        
        if (withdrawal <= balance && withdrawal > 0) {
            balance = balance - withdrawal;
            return 1;
        } elseif (withdrawal == 0 || withdrawal < 0) {
            return 0;
        }
    }
}